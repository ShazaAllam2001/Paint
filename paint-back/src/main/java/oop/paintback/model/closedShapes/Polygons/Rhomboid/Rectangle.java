package oop.paintback.model.closedShapes.Polygons.Rhomboid;

import java.awt.*;
import java.io.IOException;

public class Rectangle extends Rhomboid {

    public Rectangle(Point[] points, String outLineColor, String fillColor) throws IOException {
        super(points, outLineColor, fillColor);
    }
}