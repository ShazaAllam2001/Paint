package oop.paintback.model.closedShapes.custom;

import oop.paintback.model.closedShapes.closedShape;
import java.awt.*;

public class Drop extends closedShape {

    public Drop() { }
    public Drop(Point center) { this.center = center; }
}